<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.myfirst">
    <!-- 网络请求,还需要在application里附加,usesCleartextTraffic=true -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/icon"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/statusBar"
            android:usesCleartextTraffic="true">
        <activity android:name=".home.GuideActivity">
        </activity>
        <activity android:name=".home.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".parnterFrag.ParnterAnalysisActivity">
        </activity>
        <activity android:name=".luckFrag.LuckAnalysisActivity">
        </activity>
        <activity android:name=".starFrag.StarAnlysisActivity">
        </activity>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>